{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": [
        ".graphclient/**"
      ],
      "dependsOn": ["^build", "generate"],
      "env": []
    },
    "check": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
