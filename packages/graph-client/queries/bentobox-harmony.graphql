query HarmonyBentoBoxRebase($id: ID!) {
  HARMONY_BENTOBOX_rebase(id: $id) {
    id
    base
    elastic
  }
}
query HarmonyBentoBoxRebases($where: Rebase_filter) {
  HARMONY_BENTOBOX_rebases(where: $where) {
    id
    base
    elastic
  }
}