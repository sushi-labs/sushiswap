query EthereumBentoBoxRebase($id: ID!) {
  ETHEREUM_BENTOBOX_rebase(id: $id) {
    id
    base
    elastic
  }
}
query EthereumBentoBoxRebases($where: Rebase_filter) {
  ETHEREUM_BENTOBOX_rebases(where: $where) {
    id
    base
    elastic
  }
}