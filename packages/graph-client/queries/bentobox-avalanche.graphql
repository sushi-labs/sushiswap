query AvalancheBentoBoxRebase($id: ID!) {
  AVALANCHE_BENTOBOX_rebase(id: $id) {
    id
    base
    elastic
  }
}
query AvalancheBentoBoxRebases($where: Rebase_filter) {
  AVALANCHE_BENTOBOX_rebases(where: $where) {
    id
    base
    elastic
  }
}