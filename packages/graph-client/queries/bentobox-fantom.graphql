query FantomBentoBoxRebase($id: ID!) {
  FANTOM_BENTOBOX_rebase(id: $id) {
    id
    base
    elastic
  }
}
query FantomBentoBoxRebases($where: Rebase_filter) {
  FANTOM_BENTOBOX_rebases(where: $where) {
    id
    base
    elastic
  }
}