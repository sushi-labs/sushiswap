query OptimismBentoBoxRebase($id: ID!) {
  OPTIMISM_BENTOBOX_rebase(id: $id) {
    id
    base
    elastic
  }
}
query OptimismBentoBoxRebases($where: Rebase_filter) {
  OPTIMISM_BENTOBOX_rebases(where: $where) {
    id
    base
    elastic
  }
}