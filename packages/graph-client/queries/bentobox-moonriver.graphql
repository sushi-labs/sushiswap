query MoonriverBentoBoxRebase($id: ID!) {
  MOONRIVER_BENTOBOX_rebase(id: $id) {
    id
    base
    elastic
  }
}
query MoonriverBentoBoxRebases($where: Rebase_filter) {
  MOONRIVER_BENTOBOX_rebases(where: $where) {
    id
    base
    elastic
  }
}