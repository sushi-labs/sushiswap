query Rebase($id: ID!) {
  rebase(id: $id) {
    id
    base
    elastic
  }
}
query Rebases($where: Rebase_filter) {
  rebases(where: $where) {
    id
    base
    elastic
  }
}

query CrossChainRebases($first: Int = 1000, $where: Rebase_filter, $chainIds: [Int!]!) {
  crossChainRebases(first: $first, where: $where, chainIds: $chainIds) {
    id
    base
    elastic
  }
}
