import { OnramperButton } from './OnramperButton'
import { OnramperPanel } from './OnramperPanel'
import { OnramperProvider } from './OnramperProvider'

export const Onramper = {
  Provider: OnramperProvider,
  Button: OnramperButton,
  Panel: OnramperPanel,
}
