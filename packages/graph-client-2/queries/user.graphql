fragment UserFields on User {
  id
}

query CrossChainUserWithFarms(
  $id: ID!
  $chainIds: [Int!]!
  $block: Block_height
) {
  crossChainUserWithFarms(
    id: $id
    chainIds: $chainIds
    block: $block
  ) {
    id
    chainId
    valueUSD
    pair {
      ...PairFields
    }
  }
}