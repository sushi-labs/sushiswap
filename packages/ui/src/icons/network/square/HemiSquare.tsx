import * as React from 'react'

import { HemiNaked } from '../naked/HemiNaked'

import type { IconComponent } from '../../../types'

export const HemiSquare: IconComponent = (props) => <HemiNaked {...props} />
