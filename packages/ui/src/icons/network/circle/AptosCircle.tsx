import * as React from 'react'

import type { IconComponent } from '../../../types'
import { AptosNaked } from '../naked'

export const AptosCircle: IconComponent = (props) => <AptosNaked {...props} />
