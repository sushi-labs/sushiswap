import * as React from 'react'

import { MonadNaked } from '../naked/MonadNaked'

import type { IconComponent } from '../../../types'

export const MonadCircle: IconComponent = (props) => <MonadNaked {...props} />
