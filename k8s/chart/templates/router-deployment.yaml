{{- $chainIds := .Values.chainIds }}
{{- range $index, $value := $chainIds }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: router-{{ $value }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: router-{{ $value }}
  template:
    metadata:
      labels:
        app: router-{{ $value }}
    spec:
      containers:
        - name: router
          image: router
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "1000m"
              memory: "1Gi"
          startupProbe:
            failureThreshold: 60
            httpGet:
              path: /health
              port: 80
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 3
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /health
              port: 80
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 3
          env:
            - name: CHAIN_ID
              value: "{{ $value }}"
            - name: DRPC_ID
              valueFrom:
                secretKeyRef:
                  name: sushi-api
                  key: DRPC_ID
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef:
                  name: sushi-api
                  key: SENTRY_DSN
            - name: SENTRY_ENVIRONMENT
              valueFrom:
                secretKeyRef:
                  name: sushi-api
                  key: SENTRY_ENVIRONMENT
{{- end }}