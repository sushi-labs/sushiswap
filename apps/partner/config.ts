import { ChainId } from '@sushiswap/chain'

export const SUPPORTED_CHAINS = [
  ChainId.ETHEREUM,
  ChainId.ARBITRUM,
  ChainId.POLYGON,
  ChainId.AVALANCHE,
  ChainId.MOONRIVER,
  ChainId.FANTOM,
  ChainId.BSC,
  ChainId.GNOSIS,
  ChainId.HARMONY,
  ChainId.TELOS,
  ChainId.CELO,
  ChainId.FUSE,
  ChainId.OKEX,
  ChainId.HECO,
  ChainId.PALM,
  ChainId.KAVA,
  ChainId.METIS,
  ChainId.BOBA,
  ChainId.BOBA_AVAX,
  ChainId.ARBITRUM_NOVA,
] as const
