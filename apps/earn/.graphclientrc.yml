sources:
  - name: Earn
    handler:
      graphql:
        endpoint: https://{context.host:api.thegraph.com}/subgraphs/name/{context.name:sushi-subgraphs/sushiswap-ethereum}
    transforms:
      - autoPagination:
          validateSchema: true
plugins:
  - pollingLive:
      defaultInterval: 1000
      
documents:
  - ./query.graphql