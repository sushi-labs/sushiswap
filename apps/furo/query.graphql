query StreamsQuery {
    streams(first: 15) {
        __typename
        id
        status
        startedAt
        expiresAt
        amount
        recipient {
        id
        }
        createdBy {
        id
        }
        transactionCount
        token {
        id
        symbol
        name
        }
    }
}