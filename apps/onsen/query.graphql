query Farms {
  incentives {
    id
    token {
      id
      name
    }
    rewardToken {
      id
      name
    }
    creator {
      id
    }
    endTime
    lastRewardTime
    rewardRemaining
    liquidityStaked
  }
}

query Farm($id: ID) {
  incentive(id: $id) {
    id
    token {
      id
      name
    }
    rewardToken {
      id
      name
    }
    creator {
      id
    }
    endTime
    lastRewardTime
    rewardRemaining
    liquidityStaked
  }
}
