name: staking-kovan
handler:
  graphql:
    endpoint: https://api.thegraph.com/subgraphs/name/olastenberg/stake
transforms:
  - prefix:
      value: KOVAN_ONSEN_
      includeRootOperations: true
      # includeTypes: true
      ignore: 
        - _SubgraphErrorPolicy_
        - Token
  # - rename:
  #     - from:
  #         type: KOVAN_ONSEN_Token
  #       to:
  #         type: Tokenish
  - typeMerging:
      queryFields:
        - queryFieldName: KOVAN_ONSEN_token
          keyField: id
          keyArg: id
          # keyArg: 'where.id_in'