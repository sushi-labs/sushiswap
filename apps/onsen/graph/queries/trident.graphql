query KovanTridentPool($id: ID!) {
  KOVAN_TRIDENT_pool(id: $id) {
    id
    assets {
      token {
        id
        symbol
        name
      }
    }
    kpi {
      volume
      volumeUSD
    }
  }
}
