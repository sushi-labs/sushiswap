import { Row } from '@tanstack/react-table'
import React, { FC } from 'react'

import { SteerPosition } from '../useSteerPositions'

export const SteerNameCell: FC<Row<SteerPosition>> = ({ original }) => {
  return <div>SteerNameCell</div>
}
