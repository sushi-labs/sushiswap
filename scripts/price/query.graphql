query TokenPrices($first: Int = 5000, $skip: Int = 0, $where: Token_filter, $block: Block_height) {
  tokens(first: $first, skip: $skip, where: $where, block: $block) {
    id
    derivedETH
  }
  bundle(id: 1, block: $block) {
    ethPrice
  }
  _meta {
    block {
      number
    }
  }
}
